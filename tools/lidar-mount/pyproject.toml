[project]
name = "lidar-mount"
version = "0.1.0"
description = "RPLIDAR C1 plate on the Raspberry Pi hole pattern"
requires-python = ">=3.12"
dependencies = [
    "build123d==0.11.1",
    "numpy>=2.0",
    "trimesh>=5.0",
]

[tool.uv]
package = false
